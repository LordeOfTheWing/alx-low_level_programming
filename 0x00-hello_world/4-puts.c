#include <stdio.h>
/**
 * main - Entry point
 * use puts() to print out
 * Return: Always a 0(Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
